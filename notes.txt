I use fastapi. I have /auth/jwt/login endpoint. I need html page with 2 input field to input "username" and "password" . I need button "send" . When I click it - I have post request with body params
I need html page code

*
continue from where you left off

ivano
abc@gmail.com
abc

===

answer me on russian. I use fastapi in my service. 
@app.get("/", response_class=HTMLResponse,tags=["root"])
async def matrix_login(request: Request):
    return templates.TemplateResponse("nw_v2.html", {"request": request})

@app.get("/new_page2", response_class=HTMLResponse)
async def new_page(
    request: Request,
    fullname: str = Query("Anonim"),
    current_user: dict = Depends(get_current_user),
):
    return templates.TemplateResponse("index.html", {"request": request, "fullname": fullname})

index.html:
123

When I updated index.html I return to nw_v2.html (my root page), but I wnat to stay on nw_v2.html page. How can I do that